name: JS

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./web
    name: Lint JS

    steps:
      - uses: actions/checkout@v7

      - name: Install dependencies
        run: npm ci --include=dev

      - name: Install desktop dependencies
        run: cd ../desktop && npm ci --include=dev

      - name: Export Go data
        run: ./export-go-data.sh

      - name: Run tsc
        run: tsc --noEmit

      - name: Run ESLint
        run: npm run lint

      - name: Run tsc for desktop
        run: cd ../desktop && tsc --noEmit

      - name: Run ESLint for desktop
        run: cd ../desktop && npm run lint

      - name: Run vitest
        run: npm test
